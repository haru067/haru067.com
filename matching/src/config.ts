export interface Config {
    title: string,
    animation: {
        showUpInterval: number,
    },
};

export const config: Config  = {
    title: 'Matching',
    animation: {
        showUpInterval: 500,
    }
}
